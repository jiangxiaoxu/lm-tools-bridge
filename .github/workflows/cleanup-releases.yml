name: Cleanup old releases

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated workflow notice
        run: |
          echo "This workflow is deprecated and intentionally disabled."
          echo "Release publication now uses fixed tags: release and pre-release."
          exit 0
