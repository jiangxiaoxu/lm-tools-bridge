# Historical Or Unreachable Appendix

Historical notes stay here only and must not be described as current runtime mainline.

- Legacy HTTP manager, status page, and file-registry implementation are removed from the supported runtime path.
- Legacy helper names and `lm_getToolDefinitions` are filtered from bridged discovery and must not be exposed as callable aliases.
- Historical notes should be moved here when they no longer affect current implementation choices.

